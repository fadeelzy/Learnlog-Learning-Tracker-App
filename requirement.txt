# Core Django Framework
Django>=5.0,<6.0

# MySQL Database Connector
mysqlclient>=2.2.0

# Environment Variable Management
python-dotenv>=1.0.1

# For serving static files in production
whitenoise>=6.6.0

# Optional: Gunicorn for deploying on Render / Railway / VPS
gunicorn>=22.0.0

# For timezones, security, and general utilities
pytz>=2024.1
requests>=2.32.3

# Optional: Django Extensions (for debugging and shell_plus)
django-extensions>=3.2.3

# Optional: Form handling or frontend integration (if used)
django-crispy-forms>=2.1

# Optional: For CORS headers if you’re connecting with an external API/frontend
django-cors-headers>=4.3.1
